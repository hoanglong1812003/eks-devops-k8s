apiVersion: v1
kind: Service
metadata:
  name: multistage-app
  labels:
    app: multistage-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: multistage-app
